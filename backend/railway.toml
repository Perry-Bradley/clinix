[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python -u manage.py migrate --noinput && echo STARTING_DAPHNE && exec daphne -v 2 -b 0.0.0.0 -p 8080 clinix_project.asgi:application'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
